name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    env:
      # CI Windows runner 只有 WARP 软件设备：可用性探测通过但运行时 DXGI_ERROR_DEVICE_REMOVED。
      LEAVES_BORN_GPU: "0"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.0"
          cache-dependency-path: go.sum

      - name: go test
        run: go test ./...

      # POST-01：skills/（真源）与 .cursor/skills/（Cursor 发现镜像）哈希一致
      - name: skills mirror gate
        run: go test ./docs -run TestSkillsMirrorSync -count=1

  wasm:
    name: wasm build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.0"

      - name: build wasm demo
        run: GOOS=js GOARCH=wasm go build -o examples/wasm/leaves.wasm ./examples/wasm

      - name: wasm size gate (<= 16 MiB)
        env:
          LEAVES_WASM_GATE: "1"
        run: go test -run TestWasmBinarySizeGate -count=1 -timeout 10m

  bench-gate:
    name: backend bench gate
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.0"

      - name: batch=1 BornCPU slower than Native (>=20x)
        run: go test -run TestBenchGateBornCPUSlowerBatch1 -count=1 -timeout 5m

  race:
    name: race detector
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.0"
          cache-dependency-path: go.sum

      - name: go test -race
        run: go test -race -short ./... -count=1

  lint:
    name: lint (golangci-lint)
    runs-on: ubuntu-latest
    env:
      # 加速面（WebGPU hist/dispatch）只在 windows 构建下编译；GOOS=windows 使 lint 覆盖完整代码集。
      # 这些 helper 在 linux 构建中会被误判 unused（调用方均在 //go:build windows 文件）。
      GOOS: windows
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.0"
          cache-dependency-path: go.sum

      - uses: golangci/golangci-lint-action@v7
        with:
          version: v2.11.4
          args: --timeout 5m
