# 2026-06-13

## 制定 leaves 项目演进计划

- 全面审阅了 `c:\GitHub\leaves\`（纯 Go GBRT 预测库 v0.8.0）、`C:\GitHub\xgboost\`（参考 C++ 项目）、`C:\GitHub\gomlx\`（目标计算库 GoMLX）。
- 创建了 `演进计划.md`，规划 6 个 Phase。

## Phase 0 实施完成

### 环境
- Go 1.26.2 已安装（六倍于 GoMLX 要求的 1.25）
- go.mod 从 Go 1.12 升级到 Go 1.21

### 新增文件结构
```
leaves/
├── tree/                          # [新] 树推理通用包
│   ├── ir.go                      # ForestIR / TreeIR 通用中间表示
│   ├── engine.go                  # Engine 接口 + Backend 枚举
│   ├── native.go                  # NativeEngine 纯 Go 引擎（完整预测逻辑）
│   ├── convert.go                 # 转换辅助（LgNodeData、BuildTreeIR、变换函数）
│   └── native_test.go             # 9 个单元测试（已通过）
├── model/
│   └── ensemble.go                # [新] 基于 tree.Engine 的 Ensemble（未来 API）
├── io/
│   └── io.go                      # [新] IO 层骨架（LoadOptions、Backend、格式枚举）
├── ensemble/
│   └── ensemble.go                # [新] Booster 骨架
├── bridge.go                      # [新] 向后兼容桥（类型别名 + NewEngineFromEnsemble）
└── ...                            # 所有原有文件未改动
```

### 关键设计
- **tree.Engine 接口**：PredictDense / PredictCSR / PredictSingle / Predict / PredictLeafIndicesDense / PredictLeafIndicesCSR
- **tree.NativeEngine**：从 ForestIR 直接做预测，无需 lgTree 中间类型，完整复刻原有树遍历、分类特征、缺失值处理逻辑
- **bridge.go**：lgEnsemble/xgEnsemble → ForestIR 转换 + NewEngineFromEnsemble() 一键获取新引擎
- **根包类型别名**：TransformType、Transform、变换类型常量均指向子包

### 验证状态
- ✅ 全量编译（go build ./...）
- ✅ 全量测试 7 包通过（根包、internal/*、mat、tree、util）
- ⚠️ bridge_test.go 待重写（新旧引擎对比测试）

### 待办
- P0.7: 重写 bridge_test.go，用正确 API 加载测试数据做新旧引擎一致性验证
